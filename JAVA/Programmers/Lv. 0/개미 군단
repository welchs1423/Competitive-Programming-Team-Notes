class Solution {
    public int solution(int hp) {
        int answer = 0;
        int a = hp % 5;
        int b = a % 3;
        
        answer = hp / 5;
        answer = answer + (a / 3);
        answer = answer + b;
        return answer;
    }
}

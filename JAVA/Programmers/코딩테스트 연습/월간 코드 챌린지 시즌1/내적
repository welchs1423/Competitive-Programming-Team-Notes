class Solution {
    public int solution(int[] a, int[] b) {
        int answer = 0;
        int aLength = a.length;
        for(int i = 0; i < aLength; i++){
            answer += a[i] * b[i];
        }
        
        return answer;
    }
}
